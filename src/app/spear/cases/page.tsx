import type { Metadata } from "next";

export const metadata: Metadata = { title: "Cases | SPEAR" };

export default function CasesPage() {
  return (
    <div>
      <h1 style={{ fontSize: 22, fontWeight: 700, color: "#0F172A", marginBottom: 8 }}>Cases</h1>
      <p style={{ fontSize: 13, color: "#64748B" }}>Coming soon.</p>
    </div>
  );
}
