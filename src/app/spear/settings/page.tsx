import type { Metadata } from "next";

export const metadata: Metadata = { title: "Settings | SPEAR" };

export default function SettingsPage() {
  return (
    <div>
      <h1 style={{ fontSize: 22, fontWeight: 700, color: "#0F172A", marginBottom: 8 }}>Settings</h1>
      <p style={{ fontSize: 13, color: "#64748B" }}>Coming soon.</p>
    </div>
  );
}
