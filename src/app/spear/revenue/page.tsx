import type { Metadata } from "next";

export const metadata: Metadata = { title: "Revenue | SPEAR" };

export default function RevenuePage() {
  return (
    <div>
      <h1 style={{ fontSize: 22, fontWeight: 700, color: "#0F172A", marginBottom: 8 }}>Revenue</h1>
      <p style={{ fontSize: 13, color: "#64748B" }}>Coming soon.</p>
    </div>
  );
}
